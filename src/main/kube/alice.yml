apiVersion: kafka.strimzi.io/v1alpha1
kind: KafkaUser
metadata:
  name: alice
  labels:
    strimzi.io/cluster: my-cluster
spec:
  authentication:
    type: tls
  authorization:
    type: simple
    acls:
      - resource:
          type: topic
          name: TEST.FOO
        operation: Write
      - resource:
          type: topic
          name: TEST.FOO
        operation: Create
      - resource:
          type: topic
          name: TEST.FOO
        operation: Describe
